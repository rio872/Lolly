/* =====================================================
   LOLLY — Auth Pages JavaScript
   ===================================================== */

function togglePw() {
  var pw = document.getElementById("pw");
  pw.type = pw.type === "password" ? "text" : "password";
}
